## Setup - dev docker compose

### Setup steps

* Open Command Palette (Crtl + Shift + P) `Dev Containers: Open Folder in Container`
* Select `setup-dev-docker-compose` folder
* Init bench `../.devcontainer/init-bench.sh`
